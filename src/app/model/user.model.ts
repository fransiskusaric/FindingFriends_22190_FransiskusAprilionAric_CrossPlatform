export class User {
    id: string;
    name: string;
    nim: string;
    picture: string;
    lat: number;
    lng: number;
    constructor(id: string, name: string, nim: string, picture: string, lat: number, lng: number) {  }
}
